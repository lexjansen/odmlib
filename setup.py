"""Legacy setup.py - see pyproject.toml for package configuration.

This file is retained for backward compatibility with older pip versions
and editable installs. New installations should use:
    pip install -e .
    pip install -e ".[dev]"
"""
from setuptools import setup

setup()
